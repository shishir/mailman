default_env: &default
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: mailman
  pool: 5
  username: root
  password: root
  host: localhost

development:
  <<: *default
  database: mailman_development

test:
  <<: *default
  database: mailman_test